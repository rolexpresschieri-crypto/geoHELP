<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#C9F9B5">
    <!-- Bandiere -->
    <LinearLayout
        android:id="@+id/languageBar"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">
        <ImageView
            android:id="@+id/flagItHome"
            android:layout_width="48dp"
            android:layout_height="32dp"
            android:src="@drawable/flag_it"
            android:layout_marginBottom="8dp" />
        <ImageView
            android:id="@+id/flagEnHome"
            android:layout_width="48dp"
            android:layout_height="32dp"
            android:src="@drawable/flag_en" />
    </LinearLayout>
    <!-- Card con logo GeoHELP -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardMainLogo"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"
        android:layout_marginTop="16dp"
        app:cardCornerRadius="24dp"
        app:cardElevation="6dp"
        app:layout_constraintTop_toBottomOf="@id/languageBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintDimensionRatio="1:1">
        <ImageView
            android:id="@+id/imgMainLogo"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitCenter"
            android:src="@drawable/logo_geohelp" />
    </com.google.android.material.card.MaterialCardView>
    <!-- Testo in basso -->
    <TextView
        android:id="@+id/txtTapImages"
        android:text="@string/tap_images"
        android:textStyle="bold"
        android:textColor="@android:color/holo_blue_dark"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>